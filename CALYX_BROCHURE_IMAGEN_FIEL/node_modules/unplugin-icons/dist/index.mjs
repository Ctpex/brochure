import { a as resolveIconsPath, i as normalizeIconPath, n as generateComponentFromPath, r as isIconPath, t as resolveOptions } from "./options-Wt75Xc7M.mjs";
import { createUnplugin } from "unplugin";

//#region src/index.ts
const RE_EXTENSION = /\.\w+$/;
const RE_LEADING_SLASH = /^\//;
const unplugin = createUnplugin((options = {}) => {
	const resolved = resolveOptions(options);
	return {
		name: "unplugin-icons",
		enforce: "pre",
		resolveId(id) {
			if (isIconPath(id)) {
				const normalizedId = normalizeIconPath(id);
				const queryIndex = normalizedId.indexOf("?");
				const res = `${(queryIndex > -1 ? normalizedId.slice(0, queryIndex) : normalizedId).replace(RE_EXTENSION, "").replace(RE_LEADING_SLASH, "")}${queryIndex > -1 ? `?${normalizedId.slice(queryIndex + 1)}` : ""}`;
				const compiler = resolveIconsPath(res)?.query?.raw === "true" ? "raw" : options.compiler;
				if (compiler && typeof compiler !== "string") {
					const ext = compiler.extension;
					if (ext) return `${res}.${ext.startsWith(".") ? ext.slice(1) : ext}`;
				} else switch (compiler) {
					case "astro": return `${res}.astro`;
					case "jsx": return `${res}.jsx`;
					case "qwik": return `${res}.jsx`;
					case "marko": return `${res}.marko`;
					case "svelte": return `${res}.svelte`;
					case "solid": return `${res}.tsx`;
				}
				return res;
			}
			return null;
		},
		loadInclude(id) {
			return isIconPath(id);
		},
		async load(id) {
			const { config, resolveVirtualIconPath } = await resolved.then(({ config, resolveVirtualIconPath }) => ({
				config,
				resolveVirtualIconPath
			}));
			const result = await generateComponentFromPath(id, config);
			if (result) {
				const path = resolveVirtualIconPath(result.resolved.collection, result.resolved.icon);
				if (path) this.addWatchFile(path);
				return {
					code: result.code,
					map: {
						version: 3,
						mappings: "",
						sources: []
					}
				};
			}
		},
		rollup: { api: { config: options } },
		vite: { async handleHotUpdate(ctx) {
			const mGraph = ctx.server.moduleGraph;
			const modules = await resolved.then(({ invalidateHMR }) => invalidateHMR(ctx.file, (id) => mGraph.getModuleById(id)));
			return modules?.length ? modules : void 0;
		} }
	};
});

//#endregion
export { unplugin as default };